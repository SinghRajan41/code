 // while (a != NULL)
   // {
   //    tail->next = a;
   //    a = a->next;
   //    tail = tail->next;
   // }
   // while (b != NULL)
   // {
   //    tail->next = b;
   //    b = b->next;
   //    tail = tail->next;
   // }